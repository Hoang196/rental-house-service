const APP_CONSTANTS = {
  apiPrefix: '/rental-house',
  params: 'params',
  query: 'query',
  body: 'body',
  file: 'file',
  service: 'retal-house-service',
};

const DEFAULT_PAGING = {
  page: 1,
  page_size: 10,
};

const DEFAULT_EXPIRE_TIME = 86400;

export { APP_CONSTANTS, DEFAULT_PAGING, DEFAULT_EXPIRE_TIME };
